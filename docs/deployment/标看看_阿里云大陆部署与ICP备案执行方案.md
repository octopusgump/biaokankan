# 标看看｜阿里云大陆部署与 ICP 备案执行方案

> 文档状态：已确认执行
> 更新日期：2026-08-20
> 适用阶段：首版内部使用、中国大陆上线和首次 ICP 备案

## 1. 已经确定的事项

| 项目 | 已确定内容 |
| --- | --- |
| 品牌名 | 标看看 |
| 产品全称 | 标看看｜监理标讯助手 |
| 核心描述 | 帮监理企业更早发现值得跟进的标讯 |
| 备案主体 | 正博星元工程管理咨询有限公司 |
| 首发主域名 | `biaokankan.com` |
| 保护性备用域名 | `biaokankan.cn` |
| 云服务商 | 阿里云 |
| 当前使用范围 | 仅内部使用，不收费，不向外部客户开放注册 |
| 未来方向 | 可能收费并开放外部注册，届时单独做合规与架构升级 |

域名购买状态：

- `biaokankan.com`：已购买，阿里云控制台显示“正常”；
- `biaokankan.cn`：已购买，截至 2026-08-20 显示“注册局审核中”；
- 两个域名都要开启自动续费，但首次上线只使用 `biaokankan.com`。

## 2. 最终决策

### 2.1 新购买一台独立的中国大陆服务器

不使用现有美国（弗吉尼亚）服务器承载标看看的国内正式站。原因是：

1. 美国地域的云服务器不能作为阿里云 ICP 备案的中国大陆接入资源。
2. 现有服务器已运行 OpenClaw 及其他业务，2 核 2 GiB 内存和 40 GiB 磁盘的余量不适合再承载国内正式站。
3. 新站独立后，备案、域名、证书、日志、备份和故障影响范围都更清楚。

现有美国服务器保持不变，继续承载当前 OpenClaw 和其他已有业务。

### 2.2 命令助手不用来“区分业务”

阿里云命令助手只是向某台服务器执行命令的维护工具，不会：

- 把一台服务器变成多台独立服务器；
- 改变服务器的地域；
- 让美国服务器变成可用于 ICP 备案的中国大陆接入资源；
- 隔离内存、磁盘、网络、进程和故障。

命令助手可以在新服务器购买完成后，用来安装 Nginx、部署网站和查看运行状态。

### 2.3 域名只有一个入口

- 唯一首发入口：`https://biaokankan.com`；
- `www.biaokankan.com` 使用 301 跳转到 `https://biaokankan.com`；
- `biaokankan.cn` 在未纳入 ICP 备案前不解析、不跳转、不对外提供网站；
- `biaokankan.cn` 后续纳入备案并配置 HTTPS 后，再将所有访问 301 跳转到 `https://biaokankan.com`。

## 3. 目标架构

```text
国内用户
   │
   ▼
biaokankan.com / www.biaokankan.com
   │  阿里云 DNS，备案通过后才解析
   ▼
阿里云中国大陆轻量应用服务器
   │
   ├─ Nginx：HTTPS、域名跳转、静态文件
   ├─ 标看看前端：china-dist
   └─ 数据快照：public/data/radar.json

现有美国服务器
   └─ OpenClaw 与其他现有业务，不承载标看看国内正式站
```

首版是静态站点加 JSON 数据快照，不需要在生产环境运行数据库，也不需要为它购买大规格服务器。

## 4. 新服务器购买清单

在阿里云购买一台新的轻量应用服务器，不改造现有美国服务器。

| 购买项 | 选择 |
| --- | --- |
| 实例名称 | `biaokankan-cn-prod` |
| 地域 | 中国大陆，优先北京；如北京套餐不合适，可选青岛或张家口 |
| 规格 | 2 vCPU、2 GiB 内存 |
| 系统盘 | 40–50 GiB 或更高 |
| 公网 | 带独立公网 IPv4，套餐带宽满足首版静态站即可 |
| 购买时长 | 1 年，并开启自动续费 |
| 镜像 | Ubuntu 24.04 LTS 系统镜像，或当时受支持的 Alibaba Cloud Linux 系统镜像 |
| 应用镜像 | 不选 OpenClaw、WordPress 或其他预装应用 |
| 购买账号 | 使用当前办理域名和 ICP 备案的阿里云企业账号 |

下单前在商品页或备案控制台再确认该套餐可用于阿里云 ICP 备案。根据阿里云当前规则，中国大陆地域的轻量应用服务器包月资源需不少于 3 个月；本方案直接购买 1 年，避免时长不足。

## 5. 从现在到上线的执行顺序

### 阶段 A：完成域名实名信息同步

1. 在阿里云域名详情中确认 `biaokankan.com` 的域名持有者是“正博星元工程管理咨询有限公司”。
2. 不要用个人姓名或其他公司作为主域名持有者。
3. 确认企业实名认证已通过，域名详情中证件类型和证件号码正确。
4. 实名信息刚通过或刚变更时，等待约 3 天再提交备案，让域名信息完成同步。
5. 等待期间不将 `biaokankan.com` 解析到中国大陆服务器，也不用 IP 对外宣传。

完成标准：域名状态正常，持有者、备案主体和营业执照主体一致。

### 阶段 B：购买并初始化大陆服务器

1. 按第 4 节清单购买 `biaokankan-cn-prod`。
2. 立即绑定 SSH 密钥，不以简单密码作为长期登录方式。
3. 更新系统安全补丁，安装 Nginx、Node.js 22 和 pnpm。
4. 防火墙只开放：
   - TCP 80：公网访问，上线后用于跳转 HTTPS；
   - TCP 443：公网 HTTPS；
   - TCP 22：只允许管理员的固定 IP；如无固定 IP，需要时临时开放，用完关闭。
5. 不公开数据库、Node 开发端口、Docker 守护进程端口或其他调试端口。
6. 开启阿里云快照，首次部署完成后手动创建一份基线快照。

完成标准：服务器在中国大陆，阿里云备案系统能识别该资源，只有必要端口开放。

### 阶段 C：准备首版网站

1. 项目配置中的主域名统一设为 `biaokankan.com`。
2. 执行 `pnpm run build:china`，生成 `china-dist`。
3. 使用 Nginx 托管静态文件。推荐目录约定：

   ```text
   /var/www/biaokankan/current/       当前线上版本
   /var/www/biaokankan/releases/      历史发布版本
   /var/www/biaokankan/shared/data/   抓取数据快照
   /var/log/nginx/                    Nginx 访问与错误日志
   ```

4. 页面底部在备案通过前保持 ICP 号为空，不填写示例号码。
5. 页面明确显示：
   - 标看看；
   - 帮监理企业更早发现值得跟进的标讯；
   - 信息来自公开招标公告；
   - 以原公告和招标文件为准；
   - 备案主体全称、隐私说明和服务条款入口。
6. 首版不开放对外注册、不收费、不展示支付功能、不宣称中标保证。

首次备案审核期间，按阿里云备案页面的要求保持网站关闭或仅放置指定的备案核验页，不提前公开正式业务。

完成标准：大陆服务器内已准备好真实的标看看页面，但在备案通过前不公开。

### 阶段 D：提交阿里云 ICP 备案

在阿里云 ICP 备案系统中按以下口径填写：

| 备案字段 | 填写口径 |
| --- | --- |
| 备案类型 | 企业首次备案；如主体已有备案，按系统判断选择新增网站 |
| 主办单位 | 正博星元工程管理咨询有限公司 |
| 网站名称 | 标看看 |
| 域名 | `biaokankan.com` |
| 服务内容 | 面向工程监理企业的信息工具，整理公开招标公告中的项目名称、截止时间、招标人、代理机构等信息，提供检索和提醒；用户以原公告和招标文件为最终依据 |
| 当前经营性质 | 当前仅企业内部使用，不收费，不向外部用户开放注册 |
| 服务器 | 选择 `biaokankan-cn-prod` 对应的备案服务资源 |

注意事项：

1. 备案网站名填“标看看”，不填含有特殊符号的“标看看｜监理标讯助手”。
2. 河南企业备案按当地规则准备最新营业执照，主体负责人按要求由法定代表人办理。
3. 准备备案负责人证件、手机号、邮箱、应急联系方式和网站备案承诺书。
4. 所有信息以营业执照、域名实名认证和备案系统中的真实信息为准，不在代码或文档中保存证件照片、验证码和密码。
5. 完成阿里云初审、负责人真实性核验和管局审核；期间注意接听核验电话和查收短信。

完成标准：管局审核通过，获得真实的 ICP 备案号。

### 阶段 E：备案通过后正式上线

1. 在阿里云 DNS 中为 `biaokankan.com` 添加指向大陆服务器公网 IP 的 A 记录。
2. 为 `www.biaokankan.com` 添加指向主域名的 CNAME，或指向同一公网 IP，并在 Nginx 中跳转至无 `www` 主域名。
3. 为 `biaokankan.com` 和 `www.biaokankan.com` 申请并安装 HTTPS 证书。
4. 强制将 HTTP 301 跳转至 HTTPS，并将 `www` 301 跳转至无 `www` 主域名。
5. 在 `app/site-config.ts` 中填写审核通过的真实 ICP 备案号，重新构建和发布。
6. 确认首页底部的 ICP 备案号链接至 `https://beian.miit.gov.cn/`。
7. 在电脑、手机和不同网络下验证首页、项目列表、原公告链接、HTTPS 和数据更新时间。
8. 网站开通后，按公安机关的页面指引和时限办理公安联网备案。
9. 公安备案通过后，把公安备案号和查询链接填入项目配置，再次发布。

完成标准：`https://biaokankan.com` 可稳定访问，浏览器无证书错误，页面底部的备案信息真实且可查询。

### 阶段 F：稳定运行和数据更新

1. 保留当前“每日两次抓取”的节奏。
2. 首选将抓取任务运行在大陆服务器上，使用定时任务调用项目现有爬虫，并通过 `TENDER_SNAPSHOT_PATH` 将结果写入 Nginx 对外托管的 `data/radar.json`。
3. 抓取失败时保留上一次成功快照，不用空文件覆盖正常数据。
4. 每日查看最近一次更新时间和异常来源；每周查看磁盘、内存和 Nginx 错误日志。
5. 每周保留一份服务器快照，重大发布前另外创建手动快照。
6. 至少保留上一个可回退的网站发布版本。

## 6. `biaokankan.cn` 的处理方式

首次上线不让 `.cn` 域名参与，具体是：

1. 等待注册局审核完成并确认企业实名信息正确。
2. 保持域名不解析，不指向美国服务器或大陆服务器。
3. 开启自动续费，作为保护性域名持有。
4. `biaokankan.com` 稳定运行后，在阿里云备案系统中按当时规则为 `.cn` 办理新增网站或域名相关手续。
5. 只有 `.cn` 已纳入备案且 HTTPS 证书生效后，才配置到 `.com` 的 301 跳转。

## 7. 首版不做的事

- 不迁移、重置或删除现有美国服务器；
- 不用命令助手代替服务器隔离；
- 不在首版引入用户注册、支付、会员套餐或客户数据库；
- 不在备案通过前把主域名解析到中国大陆服务器并公开网站；
- 不填写虚假的 ICP 号或公安备案号；
- 不为 `.cn` 建设第二套网站；
- 不在本阶段做品牌视觉设计。

## 8. 上线验收清单

### 域名与证书

- [ ] `biaokankan.com` 的企业实名主体正确；
- [ ] `biaokankan.com` 指向新的中国大陆服务器；
- [ ] `http://biaokankan.com` 自动跳转到 `https://biaokankan.com`；
- [ ] `https://www.biaokankan.com` 自动跳转到 `https://biaokankan.com`；
- [ ] HTTPS 证书有效，无证书域名不匹配或过期问题；
- [ ] `biaokankan.cn` 在未纳入备案前没有公开解析。

### 备案与页面

- [ ] 备案主体是正博星元工程管理咨询有限公司；
- [ ] 备案网站名是“标看看”；
- [ ] 首页能在几秒内让用户看懂网站做什么；
- [ ] 页面底部显示真实的 ICP 备案号，并链接至工信部备案系统；
- [ ] 公安备案通过后，页面底部显示真实的公安备案号；
- [ ] 页面存在隐私说明、服务条款、主办单位和联系方式入口。

### 功能与运行

- [ ] 首页、项目列表、详情和原公告链接可用；
- [ ] 手机和电脑都能正常使用；
- [ ] 抓取时间、项目数量和数据文件一致；
- [ ] 单个信息源失败时不会清空整个网站数据；
- [ ] 阿里云监控、服务器快照和续费提醒已开启；
- [ ] 不存在对外注册、收费、支付或夸大承诺。

## 9. 将来收费和开放外部注册前的升级门槛

未来启用收费或外部客户注册时，不直接在当前首版上打开开关。先完成：

1. 根据实际收费和信息服务模式，向阿里云备案顾问或河南省通信管理局确认是否需要经营性互联网信息服务许可；
2. 补全正式用户协议、隐私政策、个人信息处理规则、账号注销和客服流程；
3. 将账号、权限、业务数据和备份迁移到适合的 ECS、RDS 和对象存储架构；
4. 接入支付前完成合同、发票、退费、对账和客服方案；
5. 进行账号安全、越权、日志留存、备份恢复和容量压力测试。

## 10. 当前的下一步

严格按以下顺序执行：

1. 等待 `biaokankan.cn` 注册局审核完成，但不用它提交首次备案。
2. 在 `biaokankan.com` 域名详情中复核企业实名持有者。
3. 购买新的阿里云中国大陆轻量应用服务器 `biaokankan-cn-prod`。
4. 确认该服务器在备案控制台中可作为接入资源。
5. 准备备案材料并在阿里云提交 `biaokankan.com` 的 ICP 备案。
6. 备案通过后，完成 DNS、HTTPS、真实备案号和网站发布。
7. 网站开通后办理公安联网备案。
8. 网站稳定后，再决定 `biaokankan.cn` 的新增备案和跳转时间。

## 11. 阿里云官方参考

- [备案服务器检查](https://help.aliyun.com/zh/icp-filing/basic-icp-service/user-guide/icp-filing-server-access-information-check)
- [ICP 备案申请概述](https://help.aliyun.com/zh/icp-filing/basic-icp-service/user-guide/icp-filing-application-overview)
- [未备案域名与不同地域服务器的访问规则](https://help.aliyun.com/zh/icp-filing/not-for-the-record-dns-can-access-to-different-areas)
- [备案流程与域名实名常见问题](https://help.aliyun.com/zh/icp-filing/basic-icp-service/support/for-the-record-process-faq)
- [河南省 ICP 备案规则](https://help.aliyun.com/zh/icp-filing/icp-filing-regulations-for-henan)
- [填写网站信息](https://help.aliyun.com/zh/icp-filing/basic-icp-service/user-guide/fill-in-website-information)
- [轻量应用服务器防火墙设置](https://help.aliyun.com/zh/simple-application-server/user-guide/manage-the-firewall-of-a-server)
- [公安联网备案信息填写指南](https://help.aliyun.com/zh/icp-filing/basic-icp-service/the-public-security-network-for-the-record-information-fill-in-the-guide/)

> 备案规则和可备案的云产品可能调整。实际提交时，以阿里云备案系统实时显示和河南省通信管理局的审核要求为准。
